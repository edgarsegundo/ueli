import Vue from "vue";


export const settingsComponent = Vue.extend({
    computed: {
    },
    data() {
        return {
        };
    },
    props: ["config", "translations"],
    mounted() {
    },
    template: `
        <h1>test</h1>
    `,
});
